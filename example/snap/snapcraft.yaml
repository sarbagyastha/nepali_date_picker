name: nepali_date_picker_example
version: 7.0.0
summary: Generated by Icons Launcher
description: Generated by Icons Launcher package for Linux.

confinement: strict
base: core22
grade: stable

slots:
  dbus-nepali_date_picker_example: # adjust accordingly to your app name
    interface: dbus
    bus: session
    name: org.bar.nepali_date_picker_example # adjust accordingly to your app name

apps:
  nepali_date_picker_example:
    command: nepali_date_picker_example
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
    - network
    slots:
      - dbus-nepali_date_picker_example
parts:
  nepali_date_picker_example:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart # The main entry-point file of the application
